stages:
  data_ingestion:
    cmd: python src/cnnClassifier/pipeline/stage_01_data_ingestion.py
    deps:
      - src/cnnClassifier/pipeline/stage_01_data_ingestion.py
      - config/config.yaml
    outs:
      - artifacts/data_ingestion/Chest-CT-Scan-data

  prepare_base_model:
    cmd: python src/cnnClassifier/pipeline/stage_02_prepare_base_model.py
    deps:
      - src/cnnClassifier/pipeline/stage_02_prepare_base_model.py
      - config/config.yaml
      - params.yaml
    params:
      - IMAGE_SIZE
      - INCLUDE_TOP
      - CLASSES
      - WEIGHTS
      - LEARNING_RATE
    outs:
      - artifacts/prepare_base_model

  # ✅ REMOVED: feature_engineering stage
  # feature_engineering:
  #   cmd: python src/cnnClassifier/pipeline/stage_03_feature_engineering.py
  #   ...

  # ✅ REMOVED: training_drift stage (optional for now)
  # training_drift:
  #   cmd: python src/cnnClassifier/pipeline/stage_04_training_drift.py
  #   ...

  # ✅ UPDATED: Training - uses raw images directly
  training:
    cmd: python src/cnnClassifier/pipeline/stage_05_model_trainer.py
    deps:
      - src/cnnClassifier/pipeline/stage_05_model_trainer.py
      - artifacts/data_ingestion/Chest-CT-Scan-data
      - artifacts/prepare_base_model
      - config/config.yaml
      - params.yaml
    params:
      - IMAGE_SIZE
      - EPOCHS
      - BATCH_SIZE
      - AUGMENTATION
      - LEARNING_RATE
    outs:
      - artifacts/training/model.h5

  # ✅ UPDATED: Evaluation
  evaluation:
    cmd: python src/cnnClassifier/pipeline/stage_06_model_evaluation.py
    deps:
      - src/cnnClassifier/pipeline/stage_06_model_evaluation.py
      - artifacts/training/model.h5
      - artifacts/data_ingestion/Chest-CT-Scan-data
      - config/config.yaml
      - params.yaml
    params:
      - IMAGE_SIZE
      - BATCH_SIZE
      - CLASSES
    metrics:
      - artifacts/evaluation_metrics.json
    outs:
      - artifacts/evaluation